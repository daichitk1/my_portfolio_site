<%= form_with(model: main_page) do |form| %>
  <% if main_page.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(main_page.errors.count, "error") %> prohibited this main_page from being saved:</h2>

      <ul>
        <% main_page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.textarea :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
